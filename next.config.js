/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/joshuariangkamang',
  images: { unoptimized: true },
  trailingSlash: false,
};

module.exports = nextConfig;
